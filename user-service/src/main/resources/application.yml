server:
  port: 8081

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=users_db;encrypt=true;trustServerCertificate=true
    username: userservice_admin
    password: UserService2025!
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
        format_sql: true

jwt:
  secret: your-secret-key-min-256-bits-long-for-hs512-algorithm
  expiration: 86400000
# 🔗 Configuration Leaderboard Service
microservices:
  leaderboard-service:
    url: http://localhost:8084

logging:
  level:
    com.userservice: DEBUG
    org.springframework.security: TRACE
    org.springframework.web: DEBUG