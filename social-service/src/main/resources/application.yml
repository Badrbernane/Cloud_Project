server:
  port: 8083

spring:
  application:
    name: social-service

  datasource:
    url: jdbc:postgresql://zlayji-social-db.cxgaw6uoyb83.us-east-2.rds.amazonaws.com:5432/postgres?sslmode=require
    username: social_admin
    password: Cloudprojet123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

# URL du User Service (pour vérifier les users)
user-service:
  url: http://localhost:8081

logging:
  level:
    com.socialservice: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
